using UnityEngine;

public class PlayerScore : MonoBehaviour
{
    [SerializeField] private Timer _timer;
    [SerializeField] private PlayerCoinsCollection _playerCoins;
    [SerializeField] private TotalCoins _totalCoins;
}
